import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    position: "absolute",
    zIndex: 2,
    width: 50,
    right: 6,
    bottom: 40,
    alignItems: "center",
  },
});

export default style;
