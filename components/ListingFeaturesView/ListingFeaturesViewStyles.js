import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    marginVertical: 6,
  },
  contentContainer: {
    paddingHorizontal: 6,
    rowGap: 6,
  },
});

export default style;
