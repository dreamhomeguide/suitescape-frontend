import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    marginVertical: 4,
  },
  contentContainer: {
    paddingHorizontal: 6,
  },
});

export default style;
