import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    flex: 1,
    left: -10,
  },
  text: {
    fontSize: 18,
    fontWeight: "500",
    color: "white",
  },
});

export default style;
