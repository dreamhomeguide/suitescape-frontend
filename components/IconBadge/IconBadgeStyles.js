import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  label: {
    color: "white",
  },
  badge: {
    position: "absolute",
    zIndex: 1,
    right: -5,
    top: -5,
  },
});

export default style;
