import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  indexContainer: {
    position: "absolute",
    left: 0,
    right: 0,
    alignItems: "center",
    zIndex: 2,
  },
  text: {
    color: "white",
  },
});

export default style;
