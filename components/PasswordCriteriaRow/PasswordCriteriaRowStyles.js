import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    flexDirection: "row",
    marginBottom: 10,
  },
  textContainer: {
    marginLeft: 5,
  },
  text: {
    fontSize: 12,
  },
});

export default style;
