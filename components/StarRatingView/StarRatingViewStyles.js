import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  ratingContainer: {
    flexDirection: "row",
    alignItems: "center",
    paddingTop: 4,
  },
  starRatingContainer: {
    marginBottom: 5,
  },
  ratingText: {
    fontSize: 12,
    marginLeft: 5,
    marginBottom: 4,
  },
  starRating: {
    marginHorizontal: 1,
  },
});

export default style;
